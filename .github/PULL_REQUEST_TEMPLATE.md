Before submitting this PR, please make sure:

- [ ] Work is done in a feature branch, with name feature/xxx-task-at-hand
- [ ] Python code is developed in package repos (except for exploratory code), and added to the project repo through git submodules
- [ ] Git submodules are added to the .gitmodules file using the SSH url
